kind = "web"
previewPath = "/"
title = "ApproveXPay Clone"
version = "1.0.0"
id = "artifacts/approvexpay-clone"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25562

[services.development]
run = "pnpm --filter @workspace/approvexpay-clone run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/approvexpay-clone", "run", "build" ]
publicDir = "artifacts/approvexpay-clone/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25562"
BASE_PATH = "/"
